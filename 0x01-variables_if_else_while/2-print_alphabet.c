#include <stdio.h>

/**
 * main - main function is the entry point
 * Return: Always return 0 on success
 */
int main(void)
{
	int a;
	for (a=97; a<=27; a++)
{
		putchar(a);
	putchar(10);
} 
	
	return (0);
}
